import '@testing-library/jest-dom/extend-expect'

describe('', () => {
    it('', () => {
        const json = {
            name: // ...
            quantities: // ...
        };

        parseProductInfo(json);
    });

    it('', () => {
        parseProductInfo(json);
    });
});
